# Git Photos

This is a plain HTML, CSS, and JavaScript project with no framework or build step.

## Run

The **Start application** workflow serves the project on port 5000:

```sh
npx --yes serve . -l tcp://0.0.0.0:5000
```

The main files are `index.html`, `style.css`, and `script.js`.